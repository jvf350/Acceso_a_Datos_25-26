-- BD hibernate que hay que tener creada antes de lanzar la app que realiza el mapeo de las 
-- clases Java a tablas relacionales

CREATE SCHEMA IF NOT EXISTS `hibernate` DEFAULT CHARACTER SET utf8 COLLATE utf8_spanish_ci ;
USE `hibernate`;